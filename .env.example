DATABASE_URL=
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
# CLERK_WEBHOOK_SECRET=

# --- Local docker e2e only (see README) — leave unset for the Vercel/Neon path ---
# SCHOLIA_AUTH=dev
# SCHOLIA_LOCAL_DB=1

# --- OpenAI (AI structure detection) ---
# Optional server-side fallback: with these set, the import form's key field can stay
# blank. Readers can always paste their own key instead (used once, never stored).
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-5.4
